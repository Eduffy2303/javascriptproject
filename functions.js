
/*
const pressGrindBeans = () => {
    console.log("Grinding for 20 seconds");
}
pressGrindBeans();



let coffeeIsGrinding = false; 

const pressGrindBeans = () => {
    if(coffeeIsGrinding) {
        console.log("stopping the grind");
        coffeeIsGrinding = false;
    } else { 
        console.log("grinding is about to begin");
        coffeeIsGrinding = true;
    }
}

pressGrindBeans();




let cashWithdrawal = (amount, accnum) => {
    console.log(`withdrawal ${amount} from ${accnum}`)
}
cashWithdrawal(10, 12873)





const order = (size, type) => {
    console.log(`order: ${size} ${type}`);
}
order("large", "americano");





let accNumber = 23677264

const cashWithdrawal = (amount, accnum) => {
    console.log(`Withdrawing ${amount} from account ${accnum}`)
}
cashWithdrawal(300, accNumber)




const addUp = (num1, num2) => {
    return num1 + num2;
}

console.log(addUp(7,3));
console.log(addUp(2,5));




const multiplyByNineFifths = (celsius) => {
    return celsius * (9/5);
};
const getFahrenheit = (celsius) => { 
    return multiplyByNineFifths(celsius) + 32;
};

console.log("The temperature is " + getFahrenheit(15) + "°F" );



function square(number) {
    return number * number;
}

square(5);





let orderCount = 0;
const takeOrder = (topping, size) => {
  console.log(`order ${orderCount}: ${size} pizza with ${topping}`);
}
takeOrder("pineapple", "large");



let balance = 100

const cashWithdrawal = (pin, amount) => {

    if (pin == 2983 && amount <= balance){
        console.log("cash withdrawn")
        //both correct
    } else if (amount > balance){
        console.log("cannot withdraw amount")
        //not enough funds
    } else {
        console.log("pin is incorrect")
        //wrong pin number
    }
}

cashWithdrawal(2985, 110)





const order = (filling1, filling2, filling3, filling4, filling5) => {
    console.log(`Your order: sandwich with ${filling1}, ${filling2}, ${filling3}, ${filling4} and ${filling5}`);
}
order("ham", "cheese", "lettuce", "tomato", "bacon");

*/














